Designing the Agent
====================

Instead of installing tools directly on the Jenkins server:

Jenkins Server

↓

Launch Docker Container

↓

Container already has all DevOps tools

↓

Run pipeline

↓

Container exits

↓

Clean environment

Every build starts from a clean environment.
